import 'package:flutter/material.dart';

class LexiaScreen extends StatelessWidget {
  const LexiaScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Lexia - AI Assistant Coming Soon'),
      ),
    );
  }
}
